import { Stack } from 'expo-router';

/** Customer routes will be introduced incrementally in the following phases. */
export default function CustomerLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
